import React from 'react';
import { Text, View } from 'react-native';

const Header = () => (
  <View>
    <Text>
      Header
    </Text>
  </View>
);

Header.propTypes = {};

export default Header;
